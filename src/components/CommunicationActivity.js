import React from 'react'

const CommunicationActivity = ({activity, activityText}) => (
    <div>
        <p>{activity}</p>
        <p>{activityText}</p>
    </div>
);

export default CommunicationActivity;